server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      git:
        uri: https://github.com/ampats04/config-server
        default-label: main
        clone-on-start: true



