server:
  port: 8088

spring:
  application:
    name: config-server

  cloud:
    server:
      git:
        uri: https://github.com/ampats04/config-server
        default-label: main
        clone-on-start: true

